# Leave unset when the API runs on localhost:8000.
# In a Codespace, set this to the forwarded URL of port 8000 (make the port Public),
# for example: https://<codespace-name>-8000.app.github.dev
VITE_API_URL=
